tooltip()
  position fixed
  z-index popupZIndex
  background-color transparentify(invBackgroundColor, 80%)
  color invTextColor
  padding 10px 15px
  font-size 12px
  line-height 12px
  white-space nowrap
  opacity 0
  transition 0.1s
  pointer-events none
